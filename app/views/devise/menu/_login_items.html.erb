<% if member_signed_in? %>
  <ul>
  	<li>Signed in as <%= current_member.email %></li>
  	<li><%= link_to('Logout', destroy_member_session_path, :method => :delete) %></li>
  	<li><%= link_to('Home', new_member_trip_path(current_member.id) ) %></li>
  </ul>
<% end %>